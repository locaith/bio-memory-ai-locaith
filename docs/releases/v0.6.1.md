# Bio-Agent OS v0.6.1

## Highlights

- split out `bio-agent-os-openclaw` as a dedicated Python package for OpenClaw-facing sidecar workflows
- added real OpenClaw and SWE-Agent config examples
- added OpenClaw bridge scaffold assets for local extension installation
- tightened detector benchmark reporting with multi-domain pairs, NLI cache metrics, precision, and false-positive tracking

## Release notes

- hybrid contradiction detector remains `8/8` on the expanded detector benchmark while heuristic remains `4/8`
- persistent NLI cache now records repeat-pass cache confirmations across real evaluation runs
- OpenClaw / SWE-Agent adoption path is now documented with copy-pasteable config examples
